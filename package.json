{
  "name": "tbat-mock-exam",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "docker:dev": "docker-compose -f docker-compose.dev.yml up --build",
    "docker:down": "docker-compose -f docker-compose.dev.yml down",
    "docker:prod": "docker-compose up --build",
    "playwright:setup": "npx playwright install && node -e \"const fs=require('fs');const path=require('path');const dir='C:/Users/User/AppData/Local/ms-playwright';if(!fs.existsSync(path.join(dir,'chromium-1179'))&&fs.existsSync(path.join(dir,'chromium-1187'))){fs.cpSync(path.join(dir,'chromium-1187'),path.join(dir,'chromium-1179'),{recursive:true});console.log('Playwright version compatibility fixed!');}else{console.log('Playwright already compatible');}\"",
    "test:e2e": "npm run playwright:setup && npx playwright test"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,css}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "shadcn": "^3.2.1",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@sentry/nextjs": "^10.10.0",
    "@types/dompurify": "^3.2.0",
    "@types/jsdom": "^21.1.7",
    "dompurify": "^3.2.6",
    "jsdom": "^26.1.0",
    "pnpm": "^10.15.1"
  }
}
